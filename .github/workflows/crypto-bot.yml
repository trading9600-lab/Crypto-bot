name: Crypto Signal Bot (MEXC)

on:
  # Automatic run every 5 minutes (UTC)
  schedule:
    - cron: "*/5 * * * *"

  # Manual run button
  workflow_dispatch:

concurrency:
  group: crypto-signal-bot
  cancel-in-progress: true

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 4

    steps:
      # 🔔 Send message when workflow starts (FIXED)
      - name: Send workflow start message
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="🚀 Crypto Signal Bot started | Trigger: ${{ github.event_name }} | UTC: $(date -u)"

      # Show trigger info in logs
      - name: Show trigger info
        run: |
          echo "Triggered by: ${{ github.event_name }}"
          date -u

      # Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # Setup Python
      - name: Set up Pyt
      
